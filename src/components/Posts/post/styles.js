import {makeStyles} from "@mui/styles";

export default makeStyles(() => ({
    cardBody: {
        width: '100%',
        zIndex: '1000',
        boxShadow: 'none',
        border: '1px solid #ccc'

    }


}))